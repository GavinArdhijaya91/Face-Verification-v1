class Visualizer:
    def draw_bbox(self, image, bbox, color=(0, 255, 255), thickness=2):
        """
        Draws bounding box on image (Cyan color as requested).
        """
        return image

    def draw_precision_grid(self, image, bbox):
        """
        Draws 8x8 precision grid over the face.
        """
        return image
